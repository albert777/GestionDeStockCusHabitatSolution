@using GestionDeStockCusHabitat.Models
@model System.Collections.Generic.List<GestionDeStockCusHabitat.Models.Utilisateur>

@{
    ViewBag.Title = "Utilisateurs";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Utilisateurs</h2>
@if (!Model.Any())
{
    <p>Pas d'utilisateur présent.</p>
}
else
{
    <table class="table table-bordered table-hover">
        <thead>
        <tr>
            <th>Nom</th>
            <th>Prenom</th>
            <th>E-mail</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var utilisateur in Model)
        {
            <tr>
                <td>@Html.ActionLink(utilisateur.Nom, "UtilisateurResult", "Utilisateurs", new {id = utilisateur.Id}, null)</td>
                <td>@Html.ActionLink(utilisateur.Prenom, "UtilisateurResult", "Utilisateurs", new {id = utilisateur.Id}, null)</td>
                <td>@Html.ActionLink(utilisateur.Email, "UtilisateurResult", "Utilisateurs", new {id = utilisateur.Id}, null)</td>
            </tr>
        }
        </tbody>
    </table>
}
